---
# 发布：远程触发目标工位 docker pull + 版本核验。见 docs/architecture.md「发布验证流程」。
# TODO: 具体任务（docker pull 指定镜像/校验和核验）待发布流程细化后补上，这里先占位跑通链路。
- name: Deploy release
  hosts: all
  gather_facts: false
  tasks:
    - name: placeholder
      ansible.builtin.debug:
        msg: "TODO: docker pull + 校验和核验，见 docs/architecture.md 发布验证流程"
